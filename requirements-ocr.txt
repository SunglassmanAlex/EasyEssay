# 可选的 OCR 依赖（处理扫描版 PDF 与图片上传）
# 安装：python -m pip install -r requirements-ocr.txt
#
# rapidocr-onnxruntime 为纯 pip 安装、无需系统依赖、自带中英文模型，推荐首选。
rapidocr-onnxruntime>=1.3
# 若已安装 Tesseract 可改用：
# pytesseract>=0.3.10
Pillow>=10.0
