# 统一换行符：仓库里一律存 LF，避免 Windows 提交的文件在 Linux/macOS 上
# 出现 "bad interpreter: ^M" 之类的怪问题（CI 就会挂在这上面）。
* text=auto eol=lf

# Windows 批处理必须是 CRLF，否则 cmd 解析会出错
*.bat text eol=crlf
*.cmd text eol=crlf

# 脚本必须是 LF（shebang 行带 CR 会让 Linux 找不到解释器）
*.sh text eol=lf

# 二进制：不要做任何换行/编码转换
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
*.svg text eol=lf
